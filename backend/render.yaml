services:
  - type: web
    name: ai-car-price-predictor-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python train_models.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0